# 研究与验证记录

## 2026-09-23 · 收束结论

根据原库本地运行结果，展示页已改为简要结论与真实排盘截图。排盘成功，AI 解读接口返回 404；当前不再继续研究或扩展模拟交互。早期网页的教学示意与交互面板已移除。以下为当时的过程记录，其中关于页面功能和“未发布”的描述仅反映 2026-09-22 的状态。

## 2026-09-22

研究提交：`9d98b3ae7fce298f751cb43c47783d6e84a7735d`。

读取上游 README、package.json、算法主流程、时间模块、格局规则、四化工具、输入表单、排盘页面、断语库占位、CI 与许可。通过 GitHub API 核对提交与 Release 文件列表；未下载 5.5 GB 样本。

### 实现

- 独立静态网页，不依赖上游服务或模型接口。
- 图示按相对偏移 4、8、6 表达三合与对宫；不展示虚构星曜或伪造个人排盘结果。
- 能力面板支持鼠标、触摸和键盘方向键/Home/End；导航、流程和场景按钮使用语义化元素。
- 使用固定提交源码链接，说明模块与 Demo 的接入差异。
- 不加载第三方字体、图片、分析脚本，无出生信息收集和服务端请求。

### 验证范围

- 构建和 JavaScript 语法检查通过，本地 HTTP 返回 200。
- 实际浏览器核验能力选项、键盘切换、宫位关系、流程详情、使用场景和样本展开说明。
- 桌面及手机宽度检查，浏览器错误日志为空。
- 未验证上游排盘准确性、未部署上游完整站点、未调用 AI、未核验全量样本。研究页本身不是上游软件复现。

### 发布

静态文件接入工作区的 web 总入口与 GitHub Pages 构建流程。没有提交或推送本次工作，不将预期公网地址记录为已发布结果。

### 全仓库校验限制

本页资源路径、内部锚点、HTML ID、源码与输出一致性、项目元信息及总入口检查均通过。全仓库 project.py sync/check 被并行创建的其他项目阻塞（当时 011-frappe-hrms 缺少 README.md）。为保留其他项目的工作，仅用现有索引中的元信息调用原索引渲染函数，更新本项目状态与截图；没有改动其他项目目录。待其他项目创建完成后可再次运行全仓库 sync/check。

## 2026-09-22 原始源库运行补充

用户要求直接查看原库真实效果。已在 .local/ziwei-doushu-upstream 安装并运行提交 9d98b3ae7fce298f751cb43c47783d6e84a7735d 的原版页面，地址 http://127.0.0.1:3010/chart。克隆内跟踪源码无修改，新增 package-lock.json。安装时一次中断造成 SWC 本机二进制不完整，单独重新安装该依赖后正常编译。启动时因受限网络对跨平台可选依赖锁修补有非致命警告，页面仍成功编译。

类型检查和五组原有回归全部通过。浏览器用非本人测试信息（1995-06-15 12:00，北京，男）完成排盘并点击武曲宫位，真实结果截图保存在 assets/upstream-chart.png。命宫与身宫子、火六局、命宫武曲/天府。AI 解读自动请求返回 404：后端未开源，未模拟成功状态。服务保持运行。此前记录的“上游未部署”是前一阶段状态，以本节实测为准。
