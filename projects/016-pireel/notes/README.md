# 实验记录

研究版本：b824ee23ff1667c45232930366cf1ef5c85318c4，2026-09-22。

## 已验证

- 浏览器实际加载真实 Studio Kit 模块，切换 6 类组件可看到不同预览与字段。
- 修改数字为 83% 后进入实际渲染。
- 在 1.4 秒拆分 4 秒片段：3 个片段变为 4 个，总时长仍为 12 秒。
- 删除拆分后的 1.4 秒片段：总时长变为 10.6 秒；撤销恢复 12 秒与 4 片段。
- 切换 9:16，预览尺寸为 540×960。
- 点击导出，实际下载 MP4；ffprobe 检查为 h264、960×540、24 fps、288 帧、12 秒。
- 从样片 5.5 秒处抽帧，实际数字图形与预览一致。
- 删除所有片段后进入空状态，播放与导出禁用；恢复示例后正常播放。
- 转写区间删除的状态从 12.0 秒变为 10.0 秒。
- 390 像素视口检查，页面宽度无横向溢出；手机布局可操作。
- 原版 Studio 启动成功；本页生成样片导入本地素材后插入画面，播放和分割正常。
- 原版导出选择 540p、24 fps、MP4，自动下载成功；ffprobe 核对 12 秒、960×540、H.264、288 帧，文件保存为 `assets/studio-export.mp4`。

## 尚未验证

- 自然语言指令、自动语音识别、云项目、素材生成。
- 真实口播、多素材复杂工程、ASR 与 AI 操作。
- Safari、Firefox 的编码和 SVG foreignObject 行为。

## 完整原版安装尝试

源文件位于被忽略的 `.local/pireel-source/pireel-b824ee23ff1667c45232930366cf1ef5c85318c4/`。
系统默认 pnpm 与上游锁文件不兼容；按上游 packageManager 指定使用 `npx --yes pnpm@9.12.0 install --frozen-lockfile` 后正常安装。大体积 `onnxruntime-web` 下载遇到网络重置，自动重试后安装成功，共耗时约 9 分钟。使用固定源树启动 Vite，未修改上游源代码。

完整原版运行于 127.0.0.1:5192。素材栏云上传显示失败，但 OPFS 本地素材可插入和播放；后台画面搜索模型需要单独下载。该状态保留在截图与本记录中，没有将云服务声明为可用。

## 展示范围

本次借用上游 pure trim 模块做可操作的最小演示。完整项目当前权威模型是 EditorDocumentV2，本示例不替代其命令层，也不将单轨实验 JSON 声称为可兼容工程。
