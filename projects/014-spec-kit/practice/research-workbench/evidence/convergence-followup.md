# 应用追踪与一次真实补漏

本次用户反馈：看不到产品如何应用Spec Kit。

## 展示修正
原页面只展示成品及文件清单，没有解释Skill规定如何落实为产品规则。新增3个具体场景×6个步骤，左右分别展示官方Skill原文与本次实际产物，并可返回源文件、代码和成品入口。

Spec Kit用于开发阶段；空间定制、比较、冲突保护都是AI根据本次需求设计和编码的功能，不是Spec Kit提供的现成组件。切换步骤是记录回看，不是现场执行AI。

## 真实问题与追踪
F1 · partial · HIGH · FR-008：两个页面同时首次定制，后提交者遭遇版本冲突，草稿仍在但“重新读取设置”因尚未配置被隐藏。

依据：spec.md中的FR-008要求冲突后允许重新读取；app.js的openSettings隐藏首次定制重读按钮。使用两个真实浏览器页面复现，trace-before-fix.json记录0通过/1失败及具体断言。测试使用独立临时SQLite。

按已安装speckit-converge的方法追加Phase 8 / T015，不降低FR-008；随后按实现阶段修复：API错误带回状态码，表单遇到409时显示重新读取，保留原草稿，重读前要求确认丢弃。

同一双页面回归修复后通过。另验证3×6个追踪节点均有真实片段、原文定位、390/1440px布局，以及键盘保存设置、进入详情并保存结论。trace-verification.json记录3通过/0失败。

## 对既有说明的补充
此前“无可操作缺口”是当时检查结论，并不能保证没有未覆盖场景；本次复核确实发现了首次定制冲突这一边界。原键盘检查只确认一次Tab产生焦点，不足以支持全面键盘可用性结论，本次补充了核心操作旅程。

原T002的未实现失败基线是API服务文件缺失，不是各项业务验收逐一失败；浏览器业务基线未记录。任务打勾不能代替执行证据。

制作新追踪页期间，一次路径错误使新增区块未写入、页面验收超时；修正后通过，原报告留在trace-page-integration-failure.json。此问题与上述用户功能缺陷分开记录。

本轮复查确认T015对应缺口已修复，未新增空任务阶段。仍无对照实验证明Spec Kit提高了开发速度；这次展示的是需求、任务、实现、验证之间可以追溯的实际作用。
