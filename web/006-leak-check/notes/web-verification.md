# 网页能力展示验证

[返回项目](../research.md)

日期：2026-09-22。仅验证本次编写的研究展示网页，不代表上游后端或在线服务的运行测试。

## 页面范围

- 六项核心能力：输入识别、精确匹配、关联扩展、字段聚合、脱敏、接口复用。
- 四步交互：识别邮箱、第一轮命中 A、第二轮关联 B、脱敏输出；C 因默认深度限制不进入结果。
- 代码、作者网站和自有数据的区别。
- 自查、自部署、已有数据、借鉴实现四种目标的价值与前提。
- 实际代码问题、验证边界及固定版本源码链接。

## 已完成检查

使用 Chromium 在本地 HTTP 预览页面完成检查：

| 检查项 | 结果 |
| --- | --- |
| 1440、768、390、320 像素宽度 | 无横向溢出 |
| 第一步到第四步 | 当前按钮状态与图中记录状态同步 |
| 两轮深度限制 | A 在第一轮命中，B 在第二轮命中，C 不命中 |
| 脱敏结果 | 显示示例打码结果，不包含 C 的邮箱 |
| 再看一遍、重置 | 回到第一步，清除命中状态 |
| 四种使用目标切换 | 对应说明、价值、前提及建议更新 |
| 五项边界说明 | 均可展开 |
| 本地文档链接 | 请求成功 |
| 手机端步骤按钮 | 可切换至脱敏结果 |
| JavaScript 运行错误 | 未发现 |
| 外部网络请求 | 初始加载和交互均无外部请求；只有用户点击外链才离开本地 |
| 视觉检查 | 桌面首屏、查询区域与手机布局截图检查 |

页面使用系统字体，无第三方运行依赖。演示内容为固定的虚构数据，没有真实个人信息输入框或查询接口。

## 构建与交付

在研究集根目录执行 `python projects/006-leak-check/scripts/build_web.py`，输出到 `web/006-leak-check/`。已加入 `web/index.html` 总入口和 GitHub Pages 构建流程。

本次未推送、未发布公网，项目的 `demo` 字段保持为空。本地页面预览端口为 5196。

## 2026-09-22 · 理解汇总与后期规划更新

新增讨论结论、面向个人用户的邮件盘点方向、邮箱适配对照及分阶段规划，并在拾迹原型顶部加入暂缓提示。状态区分连接代码、模拟验证、真实账号验证与正式可用；没有新增邮箱接入功能，也未读取真实邮件。

- 已刷新本地预览，核对汇总、产品方向、适配表和规划的实际显示。
- 1440、390、320 像素宽度下页面无横向溢出；窄屏邮箱对照表在自身容器内横向滚动。
- 页面锚点全部有目标，9 个本地页面、文档与资源链接请求成功。
- 原查询演示可切换至脱敏步骤；页面未记录 JavaScript 错误。
- 已确认原型的暂缓提示显示，并核对展示文件与源文件一致。
- 完整理解与规划文档已纳入构建输出；网页与研究文档的状态说明一致。

本次仅验证文档与展示更新，没有重复运行未修改的邮件解析或连接服务测试；上述既有测试数量仍是此前记录。

## 2026-09-22 · 单图汇总

新增 1800 × 3200 的探索全景矢量图与同内容 3600 × 6400 PNG，涵盖探索过程、原库机制、数据边界、拾迹产品方向、证据解释、用户与产品投入价值、当前状态和后期规划。网页提供内部滚动预览、大图查看与 PNG 下载，并保留当前暂停决定。

已检查图片内容、中文排版及网页内的图片加载；预览使用单一图片源，SVG 与 PNG 由同一份内容生成。图片链接和构建副本均已检查。没有改变邮箱连接或邮件分析能力。
